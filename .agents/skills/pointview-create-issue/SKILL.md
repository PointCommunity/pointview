---
name: pointview-create-issue
description: "Discuss, draft, prioritize, and create coherent PointView GitHub Issues in the Backlog. Use for new features, fixes, maintenance requests, or a laundry list that must be split into Issue-sized work."
---

# Create a PointView Issue

Read `AGENTS.md` and `.agents/pointview-pipeline-policy.html`. Read the live Project, all open Issues, and open PRs before drafting.

1. Clarify the requested outcome only where repository evidence cannot resolve it. Split unrelated goals; combine changes only when they share one coherent outcome and validation path.
2. Search open and closed Issues and PRs for duplicates or absorbed scope.
3. Draft the complete title and an HTML body with Summary, Context, Scope, Acceptance criteria, Verification, and Out of scope. Human-facing Issue content must be HTML.
4. Propose exactly one `type:*` label, applicable `area:*` labels, Priority, Impact, Effort, assignee, milestone, and Project Status.
5. Show the complete draft and metadata to the PM. Do not create or edit GitHub state until the PM explicitly approves that exact draft.
6. After approval, create the Issue open and unassigned, add it to private PointCommunity Project `PointView` number `4`, set Status `Backlog`, and populate Priority, Impact, and Effort. Leave optional fields unset unless approved.
7. Read the Issue and Project card back. Verify title, body, labels, fields, open state, inactivity, and absence of accidental duplicates.

The agent performs and verifies the Backlog transition; never ask the PM to add or move the card. Issue creation never takes the active slot, creates a branch, or changes another card's Status.
